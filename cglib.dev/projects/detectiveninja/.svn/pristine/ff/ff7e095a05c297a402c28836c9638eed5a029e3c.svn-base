#include "LoseMenu.h"

namespace proj {
	LoseMenu::LoseMenu() : Menu("LoseMenu"){
		_commands->push_back(new ResetLevelCommand());
		_commands->push_back(new ExitToMainMenuCommand());
		_commands->push_back(new ExitToDesktopCommand());
	}
	LoseMenu::~LoseMenu() {
	}

	void LoseMenu::init() {
		//Menu::init();
	}
}